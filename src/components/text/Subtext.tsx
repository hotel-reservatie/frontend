import React, { FunctionComponent } from 'react'

const Subtext: FunctionComponent = ({ children }) => {
  return <p className="text-sm text-center text-blue-400 mt-12">{children}</p>
}

export default Subtext
